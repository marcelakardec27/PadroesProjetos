// Elementos da interface da tela de login
import { CONFIG } from "../config/environment.js";

export const LoginElements = {
  URL: `${CONFIG.URL_BASE}/login`,
  INPUT_EMAIL: "#txt-login-email",
  INPUT_SENHA: "#txt-login-senha",
  BTN_ENTRAR: "#btn-entrar",
};